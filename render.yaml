services:
  - type: web
    name: traccar
    env: docker
    plan: starter
    disk:
      name: traccar-data
      mountPath: /opt/traccar/data
      sizeGB: 1
    envVars:
      - key: PORT
        value: 8080
